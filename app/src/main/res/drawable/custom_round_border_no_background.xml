<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Hình dạng màu xám cho góc trên bên trái -->
    <item android:right="16dp" android:bottom="16dp">
        <shape android:shape="rectangle">
            <solid android:color="#CCCCCC" /> <!-- Màu xám cho góc trên bên trái -->
            <corners android:bottomRightRadius="0dp" android:bottomLeftRadius="0dp" android:topRightRadius="0dp" android:topLeftRadius="16dp" />
        </shape>
    </item>

    <!-- Hình dạng màu xám cho góc trên bên phải -->
    <item android:left="16dp" android:bottom="16dp">
        <shape android:shape="rectangle">
            <solid android:color="#CCCCCC" /> <!-- Màu xám cho góc trên bên phải -->
            <corners android:bottomRightRadius="0dp" android:bottomLeftRadius="0dp" android:topRightRadius="16dp" android:topLeftRadius="0dp" />
        </shape>
    </item>

</layer-list>
